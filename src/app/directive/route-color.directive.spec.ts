import { RouteColorDirective } from './route-color.directive';

describe('RouteColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteColorDirective();
    expect(directive).toBeTruthy();
  });
});
